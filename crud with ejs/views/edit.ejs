<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>

    <div class="w-full h-full p-10 bg-zinc-900">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">read user</a>
        </div>
    <h1 class=" text-3xl text-white mb-3 tracking-tighter">Update user</h1>

        <form action="/update/<%=users._id%>" method="post">
            <input class="px-5 py-3  border-2 border-zinc-800 text-zinc-300  rounded-lg outline-none bg-transparent" value="<%=users.name%>" type="text" name="name" placeholder="name">
            <input class="px-5 py-3  border-2 border-zinc-800 text-zinc-300 rounded-lg outline-none bg-transparent" value="<%=users.email%>" type="text" name="email" placeholder="email">
            <input class="px-5 py-3  border-2 border-zinc-800 text-zinc-300 rounded-lg outline-none bg-transparent" value="<%=users.image%>" type="text" name="image" placeholder="image url"> 
            <input type="submit" value="Update user" class="px-5 py-3 bg-yellow-500 ml-3 rounded-lg text-black cursor-pointer">
        </form>
     </div>
</body>
</html>