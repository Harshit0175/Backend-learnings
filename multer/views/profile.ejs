<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full h-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end">
            <a href="/logout"
                class="text-sm  bg-red-500 text-white-500 inline-block mb-5 px-3 py-2 rounded-md">Logout</a>
        </div>
        <div class="w-20 h-20 bg-red-200 rounded-md">

        </div>
        <h3 class="text-3xl mb-5 text-white">
            <%= user.name %>
        </h3>
        <h5 class="mb-4">you can create a post</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="what's on your mind?"
                class=" p-2 w-1/3 resize-none bg-transparent border-2 border-zinc-600 rounded-md "></textarea>
            <input type="submit" value="Create New Post"
                class=" w-40 px-5 py-2 bg-blue-500  block rounded-md text-white">
        </form>
        <div class="posts mt-32">
            <h3 class="text-zinc-400 ">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.post.reverse().forEach((post)=>{%>
                    <div class="post w-1/3 p-4 mb-4 border-[1px] border-zinc-700 bg-zinc-800 rounded-md">
                        <h4 class="text-blue-500 mb-2 ">
                            <%= user.username %>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>
                        <div class="btn flex gap-4 mt-5">
                            <a class="text-blue-400" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike' %>
                            </a>
                            <a class="text-zinc-400" href="/edit/<%=post._id %>">Edit</a>
                        </div>
                    </div>




                    <% })%>
            </div>


        </div>
    </div>

</body>

</html>