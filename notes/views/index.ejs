<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <title>Document</title>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 ">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
            <input name="title" class="block px-3 py-2  rounded-lg w-full bg-zinc-800  outline-none" type="text"
                placeholder="Title goes here...">
            <textarea name="details" class="block mt-2 px-5 py-2 w-full rounded-lg resize-none bg-zinc-800 outline-none"
                placeholder="write your task details..."></textarea>
            <input type="submit" class="mt-2 bg-blue-600 px-5 py-2 rounded-md" value="create task">
            </form>

        </div>
        <div class="tasks p-10  flex gap-4 flex-wrap items-center justify-center">
            <%if(files.length>0){%>
                <% files.forEach((val)=>{ %>
                    <div class="task w-72  px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-3xl tracking-tighter"><%=val%></h1>
                        <a href="/file/<%= val %>" class="text-blue-500 inline-block mt-2">Read more...</a>
                    </div>
                    <%})%>
                        <%} else{%>
                            <h3 class="text-white">No task yet.</h3>

                            <%}%>



        </div>
    </div>

</body>
<script src="/javascripts/script.js"></script>

</html>